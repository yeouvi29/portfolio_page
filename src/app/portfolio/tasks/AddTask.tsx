"use client";

const AddTask = () => {
  return (
    <div className="w-full mt-2 p-2 group relative cursor-pointer rounded-lg hover:bg-gray-400/30 pointer-events-none">
      <p className="">+ Add a card</p>
    </div>
  );
};

export default AddTask;
